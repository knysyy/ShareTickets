import {StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    qrCodeView: {
        padding: 15,
        backgroundColor: 'white',
        overflow: 'hidden'
    },
    textView: {
        alignItems: 'center',
        marginVertical: 20,
        marginHorizontal: 40
    }
});