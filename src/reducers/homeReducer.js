import * as Types from '../actions/route/actionsTypes';

const initialState = {};

const homeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default homeReducer;